# XIAOYUAN Portfolio — Agent Guide

## 1. 项目性质

这是 XIAOYUAN 的正式求职作品集网站。

目标是尽快完成一个稳定、完整、适合招聘方浏览的作品集，而不是进行工程实验或技术展示。

项目优先级：

1. 内容完整
2. 视觉完成度
3. 手机端与桌面端浏览体验
4. 稳定性
5. 开发效率
6. 最后才是代码层面的优化

不要为了代码“更优雅”而重构已经正常工作的页面。

---

## 2. 唯一正式版本

当前打开的 GitHub 项目目录就是唯一正式工作版本。

禁止：

- 修改其他 xiaoyuan-portfolio 副本
- 修改 xiaoyuan-portfolio-codex
- 修改 Codex outputs 中的旧项目
- 使用旧 ZIP 作为工作版本
- 自行创建 LATEST / FINAL / NEW 等项目副本
- 将整个项目复制到其他目录后继续开发

所有正式修改都必须直接发生在当前项目。

---

## 3. 修改原则

每次只完成用户明确提出的任务。

如果用户要求修改一个局部功能：

- 优先定位相关文件
- 不进行无关的全项目扫描
- 不扫描上级目录
- 不扫描备份和 ZIP
- 不进行全站重构
- 不顺手修改其他页面
- 不擅自增加功能
- 不擅自改变视觉方向

能通过少量 CSS / HTML / JS 修改解决的问题，不进行架构调整。

不要因为发现“可以优化”的代码就自行优化。

---

## 4. 视觉方向

网站整体视觉语言：

- warm beige
- cream
- paper texture
- editorial
- collage
- hand-drawn
- print / magazine feeling
- 有趣但不过度幼稚
- 有个人感，不做通用模板网站

避免：

- 大面积纯黑背景
- 科技感 UI
- SaaS 风格
- 默认 AI 网站审美
- 过度圆角卡片
- 无意义渐变
- 与现有网站不一致的新视觉系统

新增设计优先复用项目已经存在的：

- 字体
- 色彩
- spacing
- paper texture
- graphic language

---

## 5. 网站主要结构

主要内容方向包括：

- Video / 视频
- Photo / 照片
- Music / 音乐人项目
- Creative / 创意策划
- Dance / 舞蹈
- About / 关于

不要未经用户确认修改栏目名称、信息架构或导航逻辑。

---

## 6. Music / Artist 项目

音乐人内容不是普通作品图片墙，而是用于展示：

- 艺人定位
- 用户参与职责
- 内容策划
- 拍摄
- 剪辑
- 发布
- 数据表现
- 项目思路
- 项目复盘

不要擅自夸大用户参与程度。

---

## 7. Egg Transition

Egg Transition 是 Music / Artist 区域的重要视觉交互。

正式 storyboard：

SELECT → LIFT → CRACK → DROP → COOK → OPEN

即：

选蛋 → 抬起 → 打破 → 落锅 → 煎好 → 打开内容

视觉标准：

- 米白纸张
- 水彩
- 拼贴
- 手绘
- editorial
- 黑色手写标注
- playful but sophisticated

禁止：

- 使用简单 CSS 椭圆代替正式鸡蛋视觉
- 使用 dotted placeholder
- TEMP SCROLL STRUCTURE
- 大面积空白测试结构
- 与参考视觉明显不一致的临时素材直接进入正式页面
- 在没有用户确认的情况下重新设计整个动画概念

如果素材尚未完成，应明确说明素材缺失，而不是自行发明新的视觉方向。

---

## 8. 响应式要求

网站必须同时考虑：

- Desktop
- Mobile

修改导航、布局、字体、交互时，需要确认受影响的桌面端和手机端表现。

但局部修改只验证受影响页面，不进行无必要的全站截图检查。

---

## 9. 性能与验证

小范围修改禁止默认执行：

- build:visual
- 全站 visual audit
- 全 assets 扫描
- 所有路由截图
- 无关页面检查

如果只是颜色、间距、字体或局部布局调整，只验证对应页面和对应 viewport。

不要启动长时间任务，除非该任务确实需要。

如果一个简单修改出现明显超出预期的执行时间，应先停止并检查原因，而不是无限继续运行。

---

## 10. Git 规则

未经用户明确确认：

- 不 Commit
- 不 Push
- 不 Deploy
- 不创建 branch
- 不 reset
- 不 revert
- 不删除用户文件

完成修改后，只说明：

- 修改了什么
- 修改了哪些文件
- 如何验证

由用户决定是否 Commit / Push。

---

## 11. 工作方式

处理任务前先判断：

“完成用户这个要求所需的最小修改是什么？”

然后只完成这个最小修改。

对于小任务：

定位 → 修改 → 局部验证 → 停止

不要：

定位 → 全站分析 → 重构 → 全站测试 → 顺便优化 → 再修改其他内容。

这个项目的核心原则是：

FINISH THE PORTFOLIO, NOT THE ENGINEERING.
