# ==============================================================
# XIAOYUAN Portfolio — 不进仓库的文件
# ==============================================================

# ---- macOS 系统文件 ----
.DS_Store
._*

# ---- 本地工作目录（AI 助手的项目记录，不需要进仓库）----
.workbuddy/

# ---- 生成 / 构建输出 ----
output/

# ---- 舞蹈原片 ----
# 这些是未经压缩的原始拍摄文件，合计约 1.67 GB，其中单个文件最大 930 MB。
# GitHub 单文件上限是 100 MB，所以这些原片**无法推送**，只能留在本地。
# 站点上「播放」按钮目前指向的就是这些原片，因此线上会提示
# 「Web 版本待补充」。要让线上能播，需要先在 assets/dance/web/ 里
# 放入 H.264 压缩版（每条约 5–10 MB），再把 app.js 里的 file 字段指过去。
assets/dance/*.MOV
assets/dance/*.mov
assets/dance/*.MP4
assets/dance/*.mp4
